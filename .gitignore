.DS_Store
**/.DS_Store
__pycache__/
*.pyc

# secrets — never commit
.env
.env.*
!.env.example

# Scratch images / office lock files accidentally left in the repo root
# (real site imagery lives in site/assets as .webp)
/*.png
/*.jpg
/*.jpeg
.~lock.*#
